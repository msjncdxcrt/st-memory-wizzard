# ============================================================
#  Memory Wizard — Git 忽略清单
#  用户数据与运行时产物不应进入公开仓库；磁盘上的文件不会被删除，
#  只是不被 git 追踪。
# ============================================================

# 用户记忆备份（每个聊天/角色的树与摘要快照，体量大、属隐私数据）
Backups/

# 运行时日志
*.log

# 网关本地运行设置（含用户选定的渠道/上游配置）
gateway/gateway-settings.json

# Claude Code 本地设置
.claude/

# 第三方配套脚本（非本插件代码 — 见 https://github.com/QinZhengqing/Cache4Chat）
酒馆助手脚本-*.json

# Node / 编辑器 / 系统
node_modules/
.env
.DS_Store
Thumbs.db
